file(GLOB_RECURSE SRC *.cpp *.c *.hpp *.h)
set(EXECUTABLE_OUTPUT_PATH  ${CMAKE_CURRENT_SOURCE_DIR})
add_executable(Planning ${SRC})
if(UNIX)
    target_link_libraries(Planning)
else()
    target_link_libraries(Planning winmm ws2_32)
endif()