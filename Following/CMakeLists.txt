file(GLOB_RECURSE SRC *.cpp *.c *.hpp *.h)
set(EXECUTABLE_OUTPUT_PATH  ${CMAKE_CURRENT_SOURCE_DIR})
add_executable(Following ${SRC})
if(UNIX)
    target_link_libraries(Following)
else()
    target_link_libraries(Following winmm ws2_32)
endif()